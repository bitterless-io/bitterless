interface:
  display_name: "Bitterless Preview / OnlyPreview"
  short_description: "OnlyPreview / only preview：预览本地文件或目录"
  default_prompt: "Use $bitterless-preview to preview the specified local file or folder through OnlyPreview (only preview)."
dependencies:
  tools:
    - type: "mcp"
      value: "bitterless"
      description: "Production Bitterless local MCP server"
      transport: "stdio"

interface:
  display_name: "Bitterless Trench"
  short_description: "Research, curate, and import Trench evidence through Bitterless"
  default_prompt: "Use $bitterless-trench to research a CA, curate a negative wallet, or import person wallets through the production Bitterless MCP server."
dependencies:
  tools:
    - type: "mcp"
      value: "bitterless"
      description: "Production Bitterless local MCP server"
      transport: "stdio"

interface:
  display_name: "Bitterless Todo"
  short_description: "Create, update, star, and organize personal synced todos"
  default_prompt: "Use $bitterless-todo to create or update my personal synced todos, including explicit star, unstar, and Focus intent, through the production Bitterless MCP server."
dependencies:
  tools:
    - type: "mcp"
      value: "bitterless"
      description: "Production Bitterless personal Todo MCP server"
      transport: "stdio"

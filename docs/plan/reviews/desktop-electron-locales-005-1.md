---
id: desktop-electron-locales-005-1
status: blocked
reviewed_task: desktop-electron-locales-005
date: 2026-07-23
review_type: independent-read-only
---

# Findings

| Priority | Blocking | Location | Finding |
| --- | --- | --- | --- |
| P1 | blocking | N/A | None. |
| P2 | blocking | `docs/plan/tasks/desktop-electron-locales-005.md:30-31,43-46`; `.gitignore:24`; `scripts/package/desktopPackageAudit.test.mjs:23-25,424-426` | The focused suite unconditionally opens `electron-builder.yml`, but that file is ignored and absent from Git (`git ls-files electron-builder.yml` returns no path). The test passes only because this working tree already contains output generated by `scripts/before.js`; a clean checkout running `yarn test:desktop-package-audit` directly will fail with `ENOENT` before it can verify the allowlist. This makes the required source test dependent on untracked workspace state. Generate/compare the config in an isolated fixture or otherwise make the check deterministic without requiring an existing ignored file. |
| P3 | non-blocking | N/A | None. |

# Conclusion

**blocked.** The Builder configuration itself is correct and the installed Electron/Builder path
does remove all but the seven requested locales before signing, but the new release-focused test is
not reproducible from a clean checkout. Resolve the P2 finding and re-run independent review.

# Evidence

- `electron-builder.tmp.yml:5-12` and the currently generated `electron-builder.yml:5-12` contain
  the exact ordered identifiers `zh_CN`, `zh_TW`, `ja`, `en`, `id`, `ko`, and `fr`; no other
  signing, notarization, audit, binary, resource, or packaging setting changed in the tracked
  Builder template.
- Electron `40.10.6` contains all seven corresponding source files under
  `Electron Framework.framework/Versions/A/Resources/<locale>.lproj/locale.pak`.
- Installed `app-builder-lib 26.7.0` reads the top-level allowlist and removes non-matching macOS
  `.lproj` directories from both application and Electron Framework resources at
  `node_modules/app-builder-lib/out/electron/ElectronFramework.js:49-77`. The packager invokes that
  step at `node_modules/app-builder-lib/out/platformPackager.js:224-235`, before `afterPack` at
  `:246-249` and signing at `:255-256`.
- An isolated invocation of that installed pruning path against the Electron `40.10.6` resources
  left exactly seven application resource directories and seven Framework resource directories:
  `en.lproj`, `fr.lproj`, `id.lproj`, `ja.lproj`, `ko.lproj`, `zh_CN.lproj`, and `zh_TW.lproj`;
  every retained Framework directory contained `locale.pak`.
- An in-memory replay of the current `scripts/before.js` Builder substitutions matched the current
  generated `electron-builder.yml` byte-for-byte.

# Verification

- `yarn test:desktop-package-audit` - PASS, 19/19 in the current working tree, where the ignored
  generated `electron-builder.yml` already exists.
- Electron `40.10.6` source locale enumeration - PASS, 7/7 requested base locale packs present.
- Installed Electron Builder locale-pruning invocation - PASS, exactly 7/7 retained in both macOS
  resource locations.
- Generator replay comparison - PASS, exact byte match.
- `git diff --check` - PASS.
- A full temporary `electron-builder --dir` package attempt did not reach packaging completion due
  to the repository's existing unresolved dependency inventory (`cannot find path for dependency
  @types/node`); this failure occurred before locale pruning and is unrelated to the reviewed diff.

# Review Boundary

This review changed only this review file. It did not modify source, Builder configuration, the task
document, or any other documentation, and it did not sign, notarize, upload, or publish an app.

/** Native SDK types for the narrow build-time CJS bridge. */
declare module 'virtual:bitterless-pi-skills' {
  export { loadSkillsFromDir, formatSkillsForPrompt, parseFrontmatter, createSyntheticSourceInfo } from '@earendil-works/pi-coding-agent'
}
